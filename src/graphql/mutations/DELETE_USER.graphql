mutation DeleteUser($deleteUserId: ID!) {
  deleteUser(id: $deleteUserId) {
    user {
      id
      name
      userName
      phone
      password
      role
    }
    error {
      message
      code
    }
  }
}