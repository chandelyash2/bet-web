query GetUsers($input: UsersInput) {
  getUsers(input: $input) {
    user {
      id
      name
      userName
      phone
      password
      role
    }
    error {
      message
      code
    }
  }
}